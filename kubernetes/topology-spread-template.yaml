# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: example-deployment
# spec:
#   template:
#     spec:
#       # Add topology spread constraints to ensure even distribution across nodes
#       topologySpreadConstraints:
#       - maxSkew: 1              # Maximum difference in number of pods between nodes
#         topologyKey: kubernetes.io/hostname  # Spread across nodes
#         whenUnsatisfiable: ScheduleAnyway   # Still schedule if constraint can't be met perfectly
#         labelSelector:
#           matchLabels:
#             app.kubernetes.io/component: example-component  # Match pods with this label
